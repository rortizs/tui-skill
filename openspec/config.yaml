schema: spec-driven
project: tui-skills
initialized: 2026-06-08
persistence:
  mode: both
  engram: true
  openspec: true
context: |
  Tech stack: Not detected; no application manifests or source files were present in the workspace.
  Architecture: Not detected; workspace currently contains SDD/agent metadata only.
  Testing: No test runner detected.
  Style: No lint, format, typecheck, or project coding conventions detected.
  Registry: .atl/skill-registry.md is present and maintained as the skill index.
delivery:
  execution_mode: auto
  chained_pr_strategy: force-chained
  review_budget_lines: 400
testing:
  strict_tdd: false
  strict_tdd_reason: "No test runner or strict TDD marker/config was detected."
  test_runner:
    available: false
    framework: null
    command: null
  test_layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality_tools:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null
rules:
  proposal:
    - Include rollback plan for risky changes.
  specs:
    - Use Given/When/Then for scenarios.
    - Use RFC 2119 keywords where normative behavior is specified.
  design:
    - Document architecture decisions with rationale.
    - State uncertainty explicitly when project context is unavailable.
  tasks:
    - Group by phase and keep tasks reviewable within the configured line budget.
  apply:
    tdd: false
    test_command: ""
    guidance:
      - Follow existing code patterns once application code exists.
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas.
