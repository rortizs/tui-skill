{
  "name": "tui-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "External skill management layer for AI coding clients.",
  "bin": {
    "tui-skills": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.8"
  }
}
